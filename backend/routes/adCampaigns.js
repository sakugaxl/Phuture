const express = require("express");
const router = express.Router();

// Placeholder: Fetch active ad campaign data
router.get("/", (req, res) => {
  res.json({ message: "Ad campaign data will be implemented here." });
});

module.exports = router;
